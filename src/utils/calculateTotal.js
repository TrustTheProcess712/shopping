//basket will be an array of objects
//need to access the price in each object of the array
//probably need to itterate over the array and push the price into a new array
//sum the total of that array

exports.calculateTotal = (basket) => {
  let total = "";
  let priceArray = [];
};
